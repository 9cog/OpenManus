# Minimal dependencies required to run the test suite in CI.
# Heavy optional packages (browser_use, playwright, docker, crawl4ai, etc.)
# are mocked via tests/conftest.py so they are NOT listed here.

# Core runtime
pydantic~=2.10.6
pydantic_core~=2.27.2
loguru~=0.7.3
pyyaml~=6.0.2
tenacity~=9.0.0
aiofiles~=24.1.0
colorama~=0.4.6
numpy
structlog

# LLM client (needed for app.llm import chain)
openai~=1.66.3
tiktoken~=0.9.0
boto3~=1.37.18
httpx>=0.27.0

# Config parsing
tomli>=2.0.0

# MCP protocol support
mcp~=1.5.0

# Web utilities (lightweight, used in some tools/tests)
beautifulsoup4~=4.13.3
requests~=2.32.3

# Testing framework
pytest~=8.3.5
pytest-asyncio~=0.25.3
